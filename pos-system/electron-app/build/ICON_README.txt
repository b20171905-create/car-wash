Put your app icon here as icon.ico (Windows requires .ico format, ideally 256x256).

If you don't have one yet, electron-builder will use a default Electron icon —
the exe will still build and work fine, just with a generic icon until you
swap this file in.

Free icon converters: https://icoconvert.com (upload a PNG, download .ico)
